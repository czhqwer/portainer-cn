# 阶段1实施进度

版本：v1.0
日期：2026-07-11
关联文档：[阶段1分批实施方案](./阶段1分批实施方案.md)、[阶段1 Goal 模式提示词模板](./阶段1Goal模式提示词模板.md)

## 1. 进度总览

| 批次 | 状态 | 提交哈希 | 验证结果 | 是否允许进入下一批 |
| --- | --- | --- | --- | --- |
| 批次 1：文档基线与 CR 收口 | 已完成 | `b9cfb93e7` | 通过 | 是 |
| 批次 2：后端核心模型与 dataservice | 已完成 | `46175c2e8` | 通过 | 是 |
| 批次 3：平台 CRUD API 与管理员权限 | 未开始 | - | - | 否 |
| 批次 4：Artifact 与 Release 控制平面 | 未开始 | - | - | 否 |
| 批次 5：前端页面骨架与部署向导 | 未开始 | - | - | 否 |
| 批次 6：Gate 0B Spike 与 Docker 执行器 | 未开始 | - | - | 否 |
| 批次 7：运行状态、日志、恢复与阶段 1 验收 | 未开始 | - | - | 否 |

## 2. 批次记录

### 批次 1：文档基线与 CR 收口

提交哈希：`b9cfb93e7`

完成内容：

- 新增 `docs/需求评审结论.md`，记录产品 CR 与技术 CR 的 Gate 0A / Gate 0B 放行结论。
- 新增 `docs/阶段1分批实施方案.md`，将阶段 1 拆成 7 批并明确批次 1 到批次 5 可先行、批次 6 到批次 7 依赖 Gate 0B。
- 新增 `docs/阶段1Goal模式提示词模板.md`，用于 Codex Goal 模式按批次自动推进阶段 1。
- 修复 PRD 和阶段 0 设计稿中的 CR 残留口径：权限范围、PRD 版本标题、成功字段清单、Release 租约字段表、健康结果字段、`RuntimeDriver.Switch` 参数和 Spike 阻断范围。

验证命令：

```powershell
rg -n 'PRD v1\.2|写操作限制管理员|V0\.1 开发前必须完成|candidate RuntimeRef|CurrentServingReleaseID`、`CurrentRuntimeRef' 'docs\阶段0平台核心模型与后端边界设计.md' 'docs\私有化微服务部署平台产品需求文档.md'
rg -n '[ \t]+$' 'docs\阶段1分批实施方案.md' 'docs\需求评审结论.md' 'docs\阶段1Goal模式提示词模板.md' 'docs\阶段0平台核心模型与后端边界设计.md' 'docs\私有化微服务部署平台产品需求文档.md'
git diff --check
```

文档链接存在性检查：

- `docs\阶段1分批实施方案.md`
- `docs\需求评审结论.md`
- `docs\阶段1Goal模式提示词模板.md`
- `docs\阶段0平台核心模型与后端边界设计.md`
- `docs\私有化微服务部署平台产品需求文档.md`
- `docs\功能优先级与工作量排期.md`

测试结果：

- 旧口径搜索无命中。
- 尾随空白搜索无命中。
- `git diff --check` 通过。
- 文档链接存在性检查通过。
- 本批只修改文档，未运行 Go 或前端测试。

遗留风险：

- 无本批阻断风险。
- 批次 6 仍必须等待 Gate 0B Docker/Agent candidate Spike 通过。

是否允许进入下一批：是。

### 批次 2：后端核心模型与 dataservice

提交哈希：`46175c2e8`

完成内容：

- 新增平台核心模型、枚举、默认值和 Gate 0A 安全的最小校验，包括 Project、PlatformEnvironment、Application、ServiceDefinition、ServiceDeployment、Artifact、Release、ReleaseStep、ReleaseLock、RuntimeRef、DesiredSpec、HealthCheckResult、GatewaySnapshot、ConfigSnapshot 等结构。
- 新增平台 dataservice 包并接入 BoltDB bucket：`platform_projects`、`platform_environments`、`platform_applications`、`platform_service_definitions`、`platform_service_deployments`、`platform_artifacts`、`platform_releases`、`platform_release_locks`。
- 接入 `dataservices.DataStore`、`Store`、`StoreTx`、Export/Import，ReleaseLock 使用 `ServiceDeploymentID` 派生字符串 key，保持同一部署同一时间只有一个活动发布锁的模型边界。
- 新增模型默认值/最小校验测试、平台 dataservice CRUD/归档测试、事务读写测试、ReleaseLock 测试和 Export/Import 测试。
- 更新迁移测试期望，补齐当前 datastore raw export 中新增的空平台 bucket。

验证命令：

```powershell
gofmt -w api/platform_models.go api/platform_models_test.go api/datastore/platform_datastore_test.go api/dataservices/interface.go api/datastore/services.go api/datastore/services_tx.go api/dataservices/platformproject/platformproject.go api/dataservices/platformenvironment/platformenvironment.go api/dataservices/platformapplication/platformapplication.go api/dataservices/platformservicedefinition/platformservicedefinition.go api/dataservices/platformservicedeployment/platformservicedeployment.go api/dataservices/platformartifact/platformartifact.go api/dataservices/platformrelease/platformrelease.go api/dataservices/platformreleaselock/platformreleaselock.go
go test ./api/datastore -run TestPlatform -v
go test ./api/datastore -run TestMigrateData -count=1
go test ./api ./api/datastore ./api/dataservices/platformproject ./api/dataservices/platformenvironment ./api/dataservices/platformapplication ./api/dataservices/platformservicedefinition ./api/dataservices/platformservicedeployment ./api/dataservices/platformartifact ./api/dataservices/platformrelease ./api/dataservices/platformreleaselock
git diff --check
```

测试结果：

- `go test ./api/datastore -run TestPlatform -v` 通过。
- `go test ./api/datastore -run TestMigrateData -count=1` 通过。
- `go test ./api ./api/datastore ./api/dataservices/platformproject ./api/dataservices/platformenvironment ./api/dataservices/platformapplication ./api/dataservices/platformservicedefinition ./api/dataservices/platformservicedeployment ./api/dataservices/platformartifact ./api/dataservices/platformrelease ./api/dataservices/platformreleaselock` 通过。
- `git diff --check` 通过，仅有 Windows 换行提示。

遗留风险：

- 本批只建立控制面模型与存储，不包含 `/api/platform` handler、管理员权限、发布状态机服务和任何 Docker 执行逻辑。
- 批次 6 仍必须等待 Gate 0B Docker/Agent candidate Spike 通过，不能提前实现 RuntimeDriver、DeploymentExecutor 或 ContainerAdapter。

是否允许进入下一批：是。
