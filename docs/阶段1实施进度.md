# 阶段1实施进度

版本：v1.0
日期：2026-07-11
关联文档：[阶段1分批实施方案](./阶段1分批实施方案.md)、[阶段1 Goal 模式提示词模板](./阶段1Goal模式提示词模板.md)

## 1. 进度总览

| 批次 | 状态 | 提交哈希 | 验证结果 | 是否允许进入下一批 |
| --- | --- | --- | --- | --- |
| 批次 1：文档基线与 CR 收口 | 已完成 | `b9cfb93e7` | 通过 | 是 |
| 批次 2：后端核心模型与 dataservice | 已完成 | `46175c2e8` | 通过 | 是 |
| 批次 3：平台 CRUD API 与管理员权限 | 已完成 | `8f5e4fd1e` | 通过 | 是 |
| 批次 4：Artifact 与 Release 控制平面 | 已完成 | `090785b8c` | 通过 | 是 |
| 批次 5：前端页面骨架与部署向导 | 已完成 | `0dcfcfad6` | 通过 | 否，进入批次 6 前必须先完成 Gate 0B Spike |
| 批次 6：Gate 0B Spike 与 Docker 执行器 | 进行中：S1/S2/S3/S6 本地子集实测通过，S7 私有 registry 子集通过，S5 本地命名子集部分验证；S4 仍待实测 | `186c71f43` / `613f2883d` / `aebb6c918` / `ff175aff0` / `8267fa139` / `4817046d8` / `b911e75ee` / `45b1cf1e1` / `122ef573d` | 本地 Docker socket、容器化 Docker socket、本地 Agent、私有 registry、清理、dry-run 和证据敏感词扫描通过；Gate 0B 整体未通过 | 否 |
| 批次 7：运行状态、日志、恢复与阶段 1 验收 | 未开始 | - | - | 否 |

## 2. 批次记录

### 批次 1：文档基线与 CR 收口

提交哈希：`b9cfb93e7`

完成内容：

- 新增 `docs/需求评审结论.md`，记录产品 CR 与技术 CR 的 Gate 0A / Gate 0B 放行结论。
- 新增 `docs/阶段1分批实施方案.md`，将阶段 1 拆成 7 批并明确批次 1 到批次 5 可先行、批次 6 到批次 7 依赖 Gate 0B。
- 新增 `docs/阶段1Goal模式提示词模板.md`，用于 Codex Goal 模式按批次自动推进阶段 1。
- 修复 PRD 和阶段 0 设计稿中的 CR 残留口径：权限范围、PRD 版本标题、成功字段清单、Release 租约字段表、健康结果字段、`RuntimeDriver.Switch` 参数和 Spike 阻断范围。

验证命令：

```powershell
rg -n 'PRD v1\.2|写操作限制管理员|V0\.1 开发前必须完成|candidate RuntimeRef|CurrentServingReleaseID`、`CurrentRuntimeRef' 'docs\阶段0平台核心模型与后端边界设计.md' 'docs\私有化微服务部署平台产品需求文档.md'
rg -n '[ \t]+$' 'docs\阶段1分批实施方案.md' 'docs\需求评审结论.md' 'docs\阶段1Goal模式提示词模板.md' 'docs\阶段0平台核心模型与后端边界设计.md' 'docs\私有化微服务部署平台产品需求文档.md'
git diff --check
```

文档链接存在性检查：

- `docs\阶段1分批实施方案.md`
- `docs\需求评审结论.md`
- `docs\阶段1Goal模式提示词模板.md`
- `docs\阶段0平台核心模型与后端边界设计.md`
- `docs\私有化微服务部署平台产品需求文档.md`
- `docs\功能优先级与工作量排期.md`

测试结果：

- 旧口径搜索无命中。
- 尾随空白搜索无命中。
- `git diff --check` 通过。
- 文档链接存在性检查通过。
- 本批只修改文档，未运行 Go 或前端测试。

遗留风险：

- 无本批阻断风险。
- 批次 6 仍必须等待 Gate 0B Docker/Agent candidate Spike 通过。

是否允许进入下一批：是。

### 批次 2：后端核心模型与 dataservice

提交哈希：`46175c2e8`

完成内容：

- 新增平台核心模型、枚举、默认值和 Gate 0A 安全的最小校验，包括 Project、PlatformEnvironment、Application、ServiceDefinition、ServiceDeployment、Artifact、Release、ReleaseStep、ReleaseLock、RuntimeRef、DesiredSpec、HealthCheckResult、GatewaySnapshot、ConfigSnapshot 等结构。
- 新增平台 dataservice 包并接入 BoltDB bucket：`platform_projects`、`platform_environments`、`platform_applications`、`platform_service_definitions`、`platform_service_deployments`、`platform_artifacts`、`platform_releases`、`platform_release_locks`。
- 接入 `dataservices.DataStore`、`Store`、`StoreTx`、Export/Import，ReleaseLock 使用 `ServiceDeploymentID` 派生字符串 key，保持同一部署同一时间只有一个活动发布锁的模型边界。
- 新增模型默认值/最小校验测试、平台 dataservice CRUD/归档测试、事务读写测试、ReleaseLock 测试和 Export/Import 测试。
- 更新迁移测试期望，补齐当前 datastore raw export 中新增的空平台 bucket。

验证命令：

```powershell
gofmt -w api/platform_models.go api/platform_models_test.go api/datastore/platform_datastore_test.go api/dataservices/interface.go api/datastore/services.go api/datastore/services_tx.go api/dataservices/platformproject/platformproject.go api/dataservices/platformenvironment/platformenvironment.go api/dataservices/platformapplication/platformapplication.go api/dataservices/platformservicedefinition/platformservicedefinition.go api/dataservices/platformservicedeployment/platformservicedeployment.go api/dataservices/platformartifact/platformartifact.go api/dataservices/platformrelease/platformrelease.go api/dataservices/platformreleaselock/platformreleaselock.go
go test ./api/datastore -run TestPlatform -v
go test ./api/datastore -run TestMigrateData -count=1
go test ./api ./api/datastore ./api/dataservices/platformproject ./api/dataservices/platformenvironment ./api/dataservices/platformapplication ./api/dataservices/platformservicedefinition ./api/dataservices/platformservicedeployment ./api/dataservices/platformartifact ./api/dataservices/platformrelease ./api/dataservices/platformreleaselock
git diff --check
```

测试结果：

- `go test ./api/datastore -run TestPlatform -v` 通过。
- `go test ./api/datastore -run TestMigrateData -count=1` 通过。
- `go test ./api ./api/datastore ./api/dataservices/platformproject ./api/dataservices/platformenvironment ./api/dataservices/platformapplication ./api/dataservices/platformservicedefinition ./api/dataservices/platformservicedeployment ./api/dataservices/platformartifact ./api/dataservices/platformrelease ./api/dataservices/platformreleaselock` 通过。
- `git diff --check` 通过，仅有 Windows 换行提示。

遗留风险：

- 本批只建立控制面模型与存储，不包含 `/api/platform` handler、管理员权限、发布状态机服务和任何 Docker 执行逻辑。
- 批次 6 仍必须等待 Gate 0B Docker/Agent candidate Spike 通过，不能提前实现 RuntimeDriver、DeploymentExecutor 或 ContainerAdapter。

是否允许进入下一批：是。

### 批次 3：平台 CRUD API 与管理员权限

提交哈希：`8f5e4fd1e`

完成内容：

- 新增 `api/http/handler/platform`，提供 Project、Environment、Application、ServiceDefinition、ServiceDeployment 的 Gate 0A CRUD API。
- 接入 `/api/platform` 根路由和 HTTP server 初始化，所有平台业务接口统一使用 `AdminAccess`，V0.1 不开放项目级 RBAC 读取。
- 实现 `ResourceVersion` 乐观锁，更新版本不匹配时返回 `PLATFORM_RESOURCE_VERSION_CONFLICT`。
- 实现 active/archived 生命周期过滤、归档人记录、父子资源归档约束和项目/环境/应用/服务/deployment 的基础唯一性校验。
- ServiceDeployment 仅保存 DesiredSpec、SpecRevision 和 DriftStatus；没有实现 RuntimeDriver、DeploymentExecutor、ContainerAdapter、candidate 验证、容器切换或恢复逻辑。
- 同步 `api/internal/testhelpers` 的假 datastore 接口，补齐批次 2 新增的平台 dataservice 访问器。

验证命令：

```powershell
gofmt -w api\http\handler\platform\handler.go api\http\handler\platform\payloads.go api\http\handler\platform\helpers.go api\http\handler\platform\projects.go api\http\handler\platform\environments.go api\http\handler\platform\applications.go api\http\handler\platform\service_definitions.go api\http\handler\platform\service_deployments.go api\http\handler\platform\platform_test.go api\http\handler\handler.go api\http\server.go api\internal\testhelpers\datastore.go
go test ./api/http/handler/platform -v
go test ./api ./api/datastore ./api/http ./api/http/handler ./api/http/handler/platform ./api/internal/testhelpers
git diff --check
```

测试结果：

- `go test ./api/http/handler/platform -v` 通过，覆盖管理员完整创建链路、普通用户拒绝、归档默认隐藏与 includeArchived 查询。
- `go test ./api ./api/datastore ./api/http ./api/http/handler ./api/http/handler/platform ./api/internal/testhelpers` 通过。
- `git diff --check` 通过，仅有 Windows 换行提示。

遗留风险：

- 本批只提供平台控制面 CRUD 和管理员鉴权，不包含 Artifact / Release 控制平面和异步发布状态机。
- 批次 6 仍必须等待 Gate 0B Docker/Agent candidate Spike 通过，不能提前实现 RuntimeDriver、DeploymentExecutor 或 ContainerAdapter。

是否允许进入下一批：是。

### 批次 4：Artifact 与 Release 控制平面

提交哈希：`090785b8c`

完成内容：

- 新增 Artifact `image-reference` 登记、列表、详情、validate 和归档 API。
- 新增 Release 列表、详情、validate 和 create 控制面 API，接入结构化 `PLATFORM_UNSUPPORTED_OPERATION` / `GATE_0B_REQUIRED` 响应。
- 实现 Release 状态机合法迁移表、终态/锁释放判定、Idempotency-Key hash、payload hash 和 24 小时幂等复用。
- `POST /api/platform/releases` 在 Gate 0B 前只写入失败 Release 事实用于幂等追踪，不创建 ReleaseLock、不启动后台 worker、不触发 Docker。
- 支持已有 active ReleaseLock 的冲突检测；同一锁和同一幂等 payload 可复用已有 Release，不同幂等键返回 `PLATFORM_RELEASE_CONFLICT`。

验证命令：

```powershell
gofmt -w api\http\handler\platform\control_plane_responses.go api\http\handler\platform\release_state.go api\http\handler\platform\artifact_release_payloads.go api\http\handler\platform\artifacts.go api\http\handler\platform\releases.go api\http\handler\platform\release_artifact_test.go api\http\handler\platform\helpers.go api\http\handler\platform\handler.go
go test ./api/http/handler/platform -v
go test ./api ./api/datastore ./api/http ./api/http/handler ./api/http/handler/platform ./api/internal/testhelpers
rg -n "NewContainerService|DockerClientFactory|RuntimeDriver|DeploymentExecutor|ContainerAdapter|candidate|PullImage|ContainerCreate|ContainerStart" api\http\handler\platform
git diff --check
```

测试结果：

- `go test ./api/http/handler/platform -v` 通过，覆盖 Artifact 生命周期、Release Gate 0B 阻断、幂等复用、payload mismatch、active lock 冲突和状态迁移表。
- `go test ./api ./api/datastore ./api/http ./api/http/handler ./api/http/handler/platform ./api/internal/testhelpers` 通过。
- Gate 0B 越界搜索未发现 Docker client、executor、容器创建或启动逻辑；仅命中 Release 快照中的 `RuntimeDriver` 字段读取。
- `git diff --check` 通过，仅有 Windows 换行提示。

遗留风险：

- 本批仍不包含后台 Docker worker、镜像拉取、digest 解析、candidate 容器、正式容器切换或恢复逻辑。
- Gate 0B 通过前，真实发布执行继续被 `GATE_0B_REQUIRED` 阻断。

是否允许进入下一批：是。

### 批次 5：前端页面骨架与部署向导

提交哈希：`0dcfcfad6`

完成内容：
- 新增 `app/react/portainer/platform` 前端页面骨架，覆盖项目、应用与服务、制品、发布记录和部署向导视图。
- 新增平台前端查询 hooks 和类型定义，按 Gate 0A 范围只读取 `/api/platform` 控制面数据。
- 接入 App Delivery 侧边栏入口、`portainer.platform.*` UI Router 状态和 r2a React 页面桥接模块。
- 部署向导先展示基础信息、已有镜像、镜像仓库、端口健康检查、环境变量、发布策略、校验结果和确认步骤。
- 在页面中明确展示 V0.1 生产风险、管理员访问限制和 Gate 0B 未通过前禁用真实发布的提示。
- 补齐平台页面英文与中文 i18n 文案。

验证命令：
```powershell
pnpm typecheck
pnpm exec prettier --check app/portainer/__module.js app/portainer/react/views/index.ts app/portainer/react/views/platform.ts app/react/sidebar/AppDeliverySidebar.tsx app/react/portainer/platform/PlatformViews.tsx app/react/portainer/platform/queries.ts app/react/portainer/platform/types.ts translations/en/translation.json translations/zh-CN/translation.json
node -e "JSON.parse(require('fs').readFileSync('translations/en/translation.json','utf8')); JSON.parse(require('fs').readFileSync('translations/zh-CN/translation.json','utf8')); console.log('translations ok')"
rg "RuntimeDriver|DeploymentExecutor|ContainerAdapter|candidate|Docker" app/react/portainer/platform app/portainer/react/views/platform.ts app/react/sidebar/AppDeliverySidebar.tsx app/portainer/__module.js
git diff --check
```

测试结果：
- `pnpm typecheck` 通过；仅提示当前 Node `v22.14.0` 低于仓库期望的 `^22.22.1`，未产生类型错误。
- `pnpm exec prettier --check ...` 通过。
- 翻译 JSON 解析通过。
- Gate 0B 越界搜索未发现 Docker 执行链路代码；命中项仅包含 Release 状态枚举和前端阻断提示文案。
- `git diff --check` 通过；仅有 Windows 换行提示。

遗留风险：
- 本批只实现前端骨架和控制面查询，不包含真实发布、candidate 容器、Docker RuntimeDriver、DeploymentExecutor 或 ContainerAdapter。
- 批次 6-7 仍依赖 Gate 0B Docker/Agent Spike；未通过前不得继续编码 Docker 执行器。

是否允许进入下一批：否。需要先完成并回填 Gate 0B Spike 结论，确认通过后才能进入批次 6。
### 批次 6：Gate 0B Spike 与 Docker 执行器（执行方案准备）

提交哈希：`186c71f43`

完成内容：
- 新增 `docs/Gate0B-Docker-Agent-Spike执行方案.md`，记录 Gate 0B Spike 的必验场景、环境准备、操作矩阵、默认策略待决项、实测记录模板和通过条件。
- 在阶段 0 设计 9.6 中补充 Spike 执行方案链接，并明确该方案不是 Gate 0B 通过结论。
- 修正阶段 0 设计 9.6 的阻断范围表述，从“阶段 1 编码前”收窄为“Docker 发布执行器正式编码前”，避免误读为阻断已放行的 Gate 0A 工作。
- 审计现有 Docker/Agent 相关代码边界：Docker client factory、Agent proxy、Docker proxy、镜像拉取、digest 解析和现有容器重建流程。

验证命令：
```powershell
rg -n "阶段 1 编码前必须完成以下 Spike|阶段1 编码前必须完成以下 Spike|V0\.1 开发前必须完成 Spike" docs
rg -n "Gate0B-Docker-Agent-Spike执行方案.md|Spike 执行方案" docs/阶段0平台核心模型与后端边界设计.md docs/Gate0B-Docker-Agent-Spike执行方案.md
git diff --check
```

测试结果：
- 旧阻断口径未在实际设计/PRD 中残留；命中项仅为需求评审结论中对历史问题的说明。
- 阶段 0 设计已链接 Spike 执行方案。
- `git diff --check` 通过；仅有 Windows 换行提示。
- 本次没有改动 Go 或前端代码，未运行 Go 测试或 `pnpm typecheck`。

遗留风险：
- Gate 0B 仍未通过。当前只完成执行方案和本地代码审计，没有真实 Docker/Agent 实测记录。
- 不得开始 Docker RuntimeDriver、DeploymentExecutor、ContainerAdapter、SingleTargetExecutor 或真实 candidate/切换/恢复代码。
- Release create 继续保持 `GATE_0B_REQUIRED` 阻断；前端真实发布入口继续禁用。

是否允许进入下一批：否。需要按 Spike 执行方案完成本地 Docker socket、容器化 Portainer、本地 Agent、远程 Agent、随机端口、版本化容器命名、正式端口切换、旧容器恢复和私有镜像认证实测，并把通过结论回填后，才能进入 Docker 执行器正式编码。
### 批次 6：Gate 0B Spike 与 Docker 执行器（本地辅助脚本）

提交哈希：`613f2883d`

完成内容：
- 新增 `docs/spike/gate0b-local-docker-spike.ps1`，作为本地 Docker socket 公开镜像子集的手工 Spike 辅助脚本。
- 脚本默认 dry-run，不执行 Docker 命令；只有显式传入 `-Apply` 才会创建测试容器。
- 脚本覆盖随机端口 candidate 健康检查、candidate 验证后清理、版本化正式容器命名、正式端口切换和旧容器恢复演练。
- 更新 `docs/Gate0B-Docker-Agent-Spike执行方案.md`，说明该脚本只覆盖 S1、S5、S6 的本地公开镜像子集，不覆盖容器化 Portainer、Agent、远程 Agent、私有 registry 和 digest 策略。

验证命令：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-local-docker-spike.ps1
git diff --check
```

测试结果：
- 脚本 dry-run 通过，仅打印执行步骤，没有调用 Docker。
- `git diff --check` 通过；仅有 Windows 换行提示。
- 本次没有运行 `-Apply`，没有创建、停止、删除任何 Docker 容器。

遗留风险：
- Gate 0B 仍未通过，S1 到 S7 仍缺真实实测记录。
- 本地脚本只降低后续实测成本，不构成发布执行器代码，也不能作为 Spike 通过证据。
- Docker RuntimeDriver、DeploymentExecutor、ContainerAdapter、SingleTargetExecutor 和真实 candidate/切换/恢复代码仍不得开始。

是否允许进入下一批：否。需要显式执行本地 Docker、容器化 Portainer、Agent、远程 Agent 和私有 registry 实测，并把通过结论回填后才能继续。
### 批次 6：Gate 0B Spike 与 Docker 执行器（实测台账）

提交哈希：`aebb6c918`

完成内容：
- 新增 `docs/spike/gate0b-spike-record.md`，作为 Gate 0B S1 到 S7 的真实证据台账。
- 台账记录每个场景的状态、证据位置、默认策略、reason 映射和 Gate 0B 结论。
- 更新 `docs/Gate0B-Docker-Agent-Spike执行方案.md`，将实测记录入口指向台账，避免模板分叉。

验证命令：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-local-docker-spike.ps1
git diff --check
```

测试结果：
- `powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-local-docker-spike.ps1` dry-run 通过；未执行 Docker。`r`n- `git diff --check` 通过；仅有 Windows 换行提示。`r`n- 三份相关文档 UTF-8 读取非空检查通过。

遗留风险：
- Gate 0B 仍未通过，台账中 S1 到 S7 均为未执行。
- 当前仍不得实现 Docker RuntimeDriver、DeploymentExecutor、ContainerAdapter、SingleTargetExecutor 或真实 candidate/切换/恢复代码。

是否允许进入下一批：否。需要执行并回填完整 Spike 实测记录后才能继续。
### 批次 6：Gate 0B Spike 与 Docker 执行器（证据采集增强）

提交哈希：`ff175aff0`

完成内容：
- 增强 `docs/spike/gate0b-local-docker-spike.ps1`，新增 `-EvidenceRoot` 参数。
- `-Apply` 执行时会写入 `metadata.txt`、`commands.txt`、`transcript.txt`、Docker version/context、测试前后容器列表、candidate/official inspect 摘要和健康检查结果。
- 修正 PowerShell native command 非零退出码不会自动进入 `catch` 的问题，避免后续实测误判预期失败场景。
- 更新 Spike 执行方案和实测台账，说明证据文件命名与敏感信息清理要求。

验证命令：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-local-docker-spike.ps1
git diff --check
node -e "const fs=require('fs'); for (const p of ['docs/Gate0B-Docker-Agent-Spike执行方案.md','docs/spike/gate0b-spike-record.md','docs/spike/gate0b-local-docker-spike.ps1']) { const text=fs.readFileSync(p,'utf8'); if (!text.trim()) throw new Error(p+' empty'); } console.log('files ok')"
```

测试结果：
- 脚本 dry-run 通过，未执行 Docker，也未创建证据目录。
- `git diff --check` 通过；仅有 Windows 换行提示。
- 相关文件 UTF-8 读取非空检查通过。

遗留风险：
- Gate 0B 仍未通过，S1 到 S7 仍缺真实实测证据。
- 本次只是增强证据采集，不是 Docker 发布执行器正式代码。
- 未获得完整 Spike 通过结论前，仍不得实现 RuntimeDriver、DeploymentExecutor、ContainerAdapter、SingleTargetExecutor 或真实 candidate/切换/恢复逻辑。

是否允许进入下一批：否。需要运行真实 Docker/Agent Spike 并回填台账、设计结论和进度文档后才能继续。
### 批次 6：Gate 0B Spike 与 Docker 执行器（证据范围收紧）

提交哈希：`8267fa139`

完成内容：
- 为 `docs/spike/gate0b-local-docker-spike.ps1` 新增 `-RecordAllContainers` 开关。
- 脚本默认只记录带 `com.portainer-cn.platform.spike=gate0b` label 的 helper 容器，避免无意采集无关用户容器名称、端口或拓扑。
- 更新 Spike 执行方案和实测台账，说明只有显式传入 `-RecordAllContainers` 才记录完整容器列表。

验证命令：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-local-docker-spike.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-local-docker-spike.ps1 -RecordAllContainers
git diff --check
```

测试结果：
- 默认 dry-run 通过，显示 `RecordAllContainers: False`，未执行 Docker。
- `-RecordAllContainers` dry-run 通过，显示 `RecordAllContainers: True`，未执行 Docker。
- `git diff --check` 通过；仅有 Windows 换行提示。

遗留风险：
- Gate 0B 仍未通过，S1 到 S7 仍缺真实实测证据。
- 本次仍只是 Spike 辅助脚本和文档，不包含 Docker 执行器正式代码。

是否允许进入下一批：否。需要运行真实 Docker/Agent Spike 并回填完整结论后才能继续。

### 批次 6：Gate 0B Spike 与 Docker 执行器（本地 Docker 实测证据）

提交哈希：`4817046d8`

完成内容：
- 修复 `docs/spike/gate0b-local-docker-spike.ps1` 中 PowerShell 参数名与 `$Args` 自动变量冲突的问题，避免 `docker version` 等证据采集命令被空参数调用。
- 改用 `docker port <container> 80/tcp` 解析 candidate 随机宿主机端口，避免 Go template 转义在 PowerShell 中不稳定。
- 写入 `docs/spike/evidence/gate0b/S1-local-docker/` 本地 Docker socket 实测证据，覆盖 candidate 随机端口健康检查、正式端口切换、坏镜像失败后旧容器恢复。
- 写入 `docs/spike/evidence/gate0b/S1-local-docker-cleanup/` 清理证据，证明 helper 容器清理后列表为空。
- 更新 `docs/spike/gate0b-spike-record.md`，标记 S1/S6 本地子集通过、S5 部分通过，并继续保留 Gate 0B 未通过结论。

验证命令：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-local-docker-spike.ps1 -Apply
powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-local-docker-spike.ps1 -Cleanup -Apply -EvidenceRoot docs/spike/evidence/gate0b/S1-local-docker-cleanup
powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-local-docker-spike.ps1
rg -n -i "password|passwd|token|authorization|registryauth|secret|private key|BEGIN [A-Z ]*KEY" docs\spike\evidence\gate0b
git diff --check
```

测试结果：
- `-Apply` 实测通过：`nginx:alpine` 拉取成功，candidate 随机端口健康检查 HTTP 200，正式端口 `18080` 上 `r1` / `r2` 健康检查 HTTP 200，坏镜像失败后 `r1` 恢复并健康检查 HTTP 200。
- `-Cleanup -Apply` 清理通过，`containers-after-cleanup.txt` 显示 helper 容器列表为空。
- dry-run 通过，未执行 Docker。
- 证据敏感词扫描无命中。
- `git diff --check` 通过；仅有 Windows 换行提示。

遗留风险：
- Gate 0B 仍未通过；S2 容器化 Portainer、S3 本地 Agent、S4 远程 Agent、S7 私有 registry 认证和 digest 解析仍缺实测证据。
- S5 只验证了本地辅助容器的 candidate/r1/r2 分离命名，尚未验证正式 `project/env/service/release` 命名模板、slug 归一化和冲突策略。
- 本次仍只是 Spike 证据和文档，不包含 Docker RuntimeDriver、DeploymentExecutor、ContainerAdapter、SingleTargetExecutor 或真实发布执行链路。

是否允许进入下一批：否。必须继续完成 S2/S3/S4/S7 和正式命名策略实测，Gate 0B 通过后才能开始 Docker 发布执行器正式编码。

### 批次 6：Gate 0B Spike 与 Docker 执行器（容器化 Docker socket 实测）

提交哈希：`b911e75ee`

完成内容：
- 新增 `docs/spike/gate0b-containerized-docker-spike.ps1`，用于验证容器化 Portainer + Docker socket 的本地网络子集。
- 脚本默认 dry-run，只有 `-Apply` 时才创建 candidate 和 probe 容器；默认证据只记录 Spike helper 容器。
- 写入 `docs/spike/evidence/gate0b/S2-containerized-docker/` 实测证据，覆盖容器内 Docker socket、candidate 随机端口解析、`127.0.0.1`、Docker bridge gateway 和 `host.docker.internal` 可达性。
- 更新 Gate 0B 执行方案和实测台账，将 S2 标记为本地子集通过，并继续保留 Gate 0B 未通过结论。

验证命令：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-containerized-docker-spike.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-containerized-docker-spike.ps1 -Apply
rg -n "Registry@123456|minio123|registryadmin|Authorization:|RegistryAuth" docs\spike\evidence\gate0b\S2-containerized-docker docs\spike\gate0b-containerized-docker-spike.ps1 docs\Gate0B-Docker-Agent-Spike执行方案.md docs\spike\gate0b-spike-record.md
git diff --check
```

测试结果：
- dry-run 通过，未执行 Docker。
- `-Apply` 实测通过：容器内 Docker CLI 挂载 `/var/run/docker.sock` 后可访问 Docker Engine。
- candidate 随机宿主机端口解析成功。
- probe 容器内访问 `127.0.0.1:<port>` 失败，符合容器内 loopback 不等于宿主机的预期。
- probe 容器内访问 Docker bridge gateway `172.17.0.1:<port>` 和 `host.docker.internal:<port>` 均返回 HTTP 200。
- helper 容器最终列表为空。
- 真实 Registry/Minio 密码和认证头扫描无命中。

遗留风险：
- Gate 0B 仍未通过；S3 本地 Agent、S4 远程 Agent、S7 私有 registry 认证和 digest 解析仍缺实测证据。
- S2 结论只覆盖本地 Docker Desktop + bridge 网络子集，正式实现不能硬编码 `172.17.0.1` 或 `host.docker.internal`，仍需支持显式配置 `HealthCheckHost`。
- 本次仍只是 Spike 证据和文档，不包含 Docker RuntimeDriver、DeploymentExecutor、ContainerAdapter、SingleTargetExecutor 或真实发布执行链路。

是否允许进入下一批：否。必须继续完成 S3/S4/S7 和正式命名策略实测，Gate 0B 通过后才能开始 Docker 发布执行器正式编码。

### 批次 6：Gate 0B Spike 与 Docker 执行器（私有 Registry 实测）

提交哈希：`45b1cf1e1`

完成内容：
- 新增 `docs/spike/gate0b-private-registry-spike.ps1`，用于验证私有 registry 认证、错误凭据、缺失镜像和 digest 解析。
- 脚本使用临时 Docker config 和父 shell 环境变量传入凭据，commands/transcript 中仅记录 `<redacted>`，执行完成后删除临时认证目录。
- 写入 `docs/spike/evidence/gate0b/S7-private-registry/` 实测证据，覆盖正确凭据 push/pull、错误凭据 401、缺失镜像 manifest unknown、pull 后 RepoDigests 解析。
- 更新 Gate 0B 执行方案和实测台账，将 S7 标记为通过，并继续保留 Gate 0B 未通过结论。

验证命令：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-private-registry-spike.ps1
$env:GATE0B_REGISTRY_USERNAME = '<registry-username>'
$env:GATE0B_REGISTRY_PASSWORD = '<registry-password>'
powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-private-registry-spike.ps1 -Apply
rg -n "Registry@123456|minio123|registryadmin|cmVnaXN0cnlhZG1pbg|UmVnaXN0cnlAMTIzNDU2|Authorization:|RegistryAuth|auths|identitytoken" docs\spike\evidence\gate0b\S7-private-registry docs\spike\gate0b-private-registry-spike.ps1 docs\Gate0B-Docker-Agent-Spike执行方案.md docs\spike\gate0b-spike-record.md
git diff --check
```

测试结果：
- dry-run 通过，未执行 Docker。
- 正确凭据登录本地 registry 成功。
- `nginx:alpine` tag 为 `127.0.0.1:5000/portainer-cn/gate0b-spike:v0.1` 后 push 成功。
- 使用正确凭据 pull 私有镜像成功，`RepoDigests` 解析到 `127.0.0.1:5000/portainer-cn/gate0b-spike@sha256:2fabf6963cb8eb9f6806beac013d5b4c347dcb254e54aef4bdafd61fa6a06d17`。
- 错误凭据登录返回 401 Unauthorized，可映射为 `REGISTRY_AUTH_FAILED`。
- 缺失镜像 pull 返回 `manifest unknown`，可映射为 `IMAGE_PULL_FAILED`。
- 临时 Docker config 已删除，本地私有镜像 tag 已清理。
- 真实 Registry/Minio 密码、用户名、base64 片段、认证头和 Docker config 关键字扫描无命中。

遗留风险：
- Gate 0B 仍未通过；S3 本地 Agent、S4 远程 Agent 和正式命名策略仍缺实测证据。
- 本地 HTTP registry 下 `docker manifest inspect` 返回 `no such manifest`，正式实现应以 pull 后 RepoDigests 为权威 digest 来源，不能只依赖 manifest inspect。
- 本次仍只是 Spike 证据和文档，不包含 Docker RuntimeDriver、DeploymentExecutor、ContainerAdapter、SingleTargetExecutor 或真实发布执行链路。

是否允许进入下一批：否。必须继续完成 S3/S4 和正式命名策略实测，Gate 0B 通过后才能开始 Docker 发布执行器正式编码。

### 批次 6：Gate 0B Spike 与 Docker 执行器（本地 Agent 实测）

提交哈希：`122ef573d`

完成内容：
- 新增 `docs/spike/gate0b-local-agent-spike.ps1`，用于验证本地 Agent 启动、`/ping`、Docker platform header、candidate 随机端口从 Portainer 主机可达性。
- 脚本默认 dry-run，只有 `-Apply` 时才启动 Agent 和 candidate helper 容器；结束时清理 helper 容器。
- 写入 `docs/spike/evidence/gate0b/S3-local-agent/` 实测证据，覆盖 Agent version/platform、candidate 随机端口、健康检查和清理结果。
- 更新 Gate 0B 执行方案和实测台账，将 S3 标记为本地子集通过，并继续保留 Gate 0B 未通过结论。

验证命令：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-local-agent-spike.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File docs/spike/gate0b-local-agent-spike.ps1 -Apply
rg -n "Registry@123456|minio123|registryadmin|Authorization:|RegistryAuth|auths|identitytoken" docs\spike\evidence\gate0b\S3-local-agent docs\spike\gate0b-local-agent-spike.ps1 docs\Gate0B-Docker-Agent-Spike执行方案.md docs\spike\gate0b-spike-record.md
git diff --check
```

测试结果：
- dry-run 通过，未执行 Docker。
- `portainer/agent:2.43.0` 启动成功。
- Agent `/ping` 返回 HTTP 204，响应头包含 `Portainer-Agent: 2.43.0` 和 `Portainer-Agent-Platform: 1`。
- candidate 随机宿主机端口从当前 Portainer 主机通过 `127.0.0.1` 访问返回 HTTP 200。
- helper 容器最终列表为空。
- 真实 Registry/Minio 密码、用户名和认证头扫描无命中。

遗留风险：
- Gate 0B 仍未通过；S4 远程 Agent、多节点 `X-PortainerAgent-Target` 目标选择和正式命名策略仍缺实测证据。
- 本地 Agent 脚本未覆盖真实 Portainer 后端签名 Docker API 调用；执行器编码前仍需在后端联调中验证签名请求链路。
- 本次仍只是 Spike 证据和文档，不包含 Docker RuntimeDriver、DeploymentExecutor、ContainerAdapter、SingleTargetExecutor 或真实发布执行链路。

是否允许进入下一批：否。必须继续完成 S4 和正式命名策略实测，Gate 0B 通过后才能开始 Docker 发布执行器正式编码。
