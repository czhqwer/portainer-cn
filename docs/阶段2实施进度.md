# 阶段 2 实施进度

版本：v1.0
启动日期：2026-07-12
关联文档：[阶段2分批实施方案与Goal模式提示词](./阶段2分批实施方案与Goal模式提示词.md)、[阶段1实施进度](./阶段1实施进度.md)、[阶段0平台核心模型与后端边界设计](./阶段0平台核心模型与后端边界设计.md)、[私有化微服务部署平台产品需求文档](./私有化微服务部署平台产品需求文档.md)

## 1. 进度总览

| 批次 | 状态 | 提交哈希 | 验证结果 | 是否允许进入下一批 |
| --- | --- | --- | --- | --- |
| 批次 1：阶段 2 基线审计与方案冻结 | 已完成 | `051c6429c` | 通过 | 是 |
| 批次 2：配置中心模型与 dataservice | 已完成 | `ffe0c2124` | 通过 | 是 |
| 批次 3：配置中心 API 与配置合并 | 已完成 | `2804e54ee` | 通过 | 是 |
| 批次 4：敏感变量加密、脱敏与注入 | 已完成 | `ec919674d` | 通过 | 是 |
| 批次 5：基础 RBAC 与 Endpoint 权限交集 | 已完成 | `d359d0084` | 通过 | 是 |
| 批次 6：结构化平台审计扩展 | 未开始 | - | - | 否 |
| 批次 7：人工历史版本回滚 | 未开始 | - | - | 否 |
| 批次 8：前端整合与阶段 2 验收 | 未开始 | - | - | 否 |

## 2. 批次 1：基线审计与方案冻结

### 2.1 目标、范围与边界

本批只冻结阶段 2 的配置、安全、权限、审计和人工回滚边界；不改业务代码、不开放非管理员访问、不实现配置或回滚 API。

阶段 2 严禁提前实现以下能力：Jar、dist、镜像 tar 或 OCI archive 的构建/导入，MinIO/S3 制品拉取，向已有 registry 推送标准 tag，中心 Nginx、域名、证书或网关切流，多 Docker 主机或跨主机分批发布，以及 Kubernetes、Git、源码构建、内置 registry、完整 CI/CD、审批流。

敏感变量在后续批次中只能以受 Portainer 加密能力保护的密文保存；明文不得进入 BoltDB 非加密字段、日志、审计、错误响应、前端 toast 或测试快照。

### 2.2 审计结论

- 阶段 1 已完成并提供 Docker 单机已有镜像的发布、运行态、日志、失败自动恢复和最小发布审计闭环；其进度文档给出的最后收口提交为 `e985e3cd1`、`8c4386323`、`9497405a5`。
- 当前分支为 `codex/platform-phase2`，审计时工作区干净；最近阶段 2 相关提交仅为 `7bf606ec8 docs: 新增阶段2Goal模式提示词`，没有任何阶段 2 批次的完成或测试证据。
- `api/platform_models.go` 已预留 `PlatformConfigSet`、`PlatformConfigEntry`、`PlatformSecretRef`、`PlatformEffectiveConfigSnapshot`、`PlatformSecretSnapshot`、`RollbackSourceReleaseID`、项目成员/团队角色和 `PlatformAuditLog` 字段。这些只是模型预留：尚未发现 ConfigSet 的 dataservice、Bucket、迁移、Export/Import、配置 CRUD、合并计算、敏感变量加密或历史回滚实现。
- 阶段 1 已存在 `platform_audit_logs` dataservice 及只读 `/api/platform/audit-logs`，只覆盖发布和人工处置的最小审计；配置、安全、权限、denied 和回滚审计仍是后续缺口。
- `api/http/handler/platform/handler.go` 目前全部 `/api/platform` 业务路由使用 `AdminAccess`。阶段 2 必须在服务端以项目角色和 Endpoint 访问权限的交集替代这一全局限制，前端隐藏只能作为体验优化。

因此阶段 2 从批次 1 开始；批次 2 的前置条件是在本批文档冻结、验证和提交后满足。

### 2.3 冻结的数据与安全约束

- 配置集作用域固定为 `project`、`environment`、`service-deployment`；默认配置集名为 `default`。一个作用域的同名有效配置集必须唯一。
- 有效普通配置的覆盖顺序固定为：项目级 < 环境级 < 服务部署级 `EnvOverrides`。同名后层覆盖前层，输出和哈希必须使用稳定排序。
- 发布快照必须包含 `SpecRevision`、参与合并的 `ConfigSetRevisions`、脱敏后的 `Entries`、`ConfigHash` 与 `SecretSnapshots`。删除或修改现有配置不能改写历史 Release 快照。
- 敏感数据的 API 默认仅返回变量名、类型、是否有值、hash/版本和字段名；reveal/copy 单独鉴权且写入审计。`SecretSnapshots` 仅保存变量名、密钥版本、密文或可回滚引用和密文 hash。
- 配置漂移定义为当前有效配置 hash 或任一参与配置集 revision 与当前成功 Release 快照不一致；服务部署状态使用既有 `config-changed`，不覆盖 `runtime-missing` 等运行态漂移。
- 人工回滚必须从一个历史成功且可追溯的 Release 创建新的 `TriggerType=rollback` Release，并填写 `RollbackSourceReleaseID`。预检失败或执行失败不得改写现有 Release，也不得破坏当前运行版本。

### 2.4 API 契约草案

以下路径以既有 `/api/platform` 路由风格为基线；批次实现时仍需按现有 handler 的错误 envelope、资源版本和审计辅助函数落地。

| 批次 | 路径 | 目的 | 权限与脱敏要求 |
| --- | --- | --- | --- |
| 3 | `GET/POST /api/platform/config-sets` | 按项目、作用域、作用域 ID 查询或创建配置集 | 读取遵循项目查看权；写入要求项目管理员；响应不回传敏感明文/密文 |
| 3 | `GET/PUT/DELETE /api/platform/config-sets/{configSetId}` | 查询、更新或归档配置集 | 更新使用 `ResourceVersion`；内容变化递增 `Revision` |
| 3 | `GET /api/platform/service-deployments/{deploymentId}/effective-config` | 返回三级合并后的有效配置和 hash | 仅返回脱敏条目；服务部署不可见时按权限矩阵返回 404 |
| 4 | `POST /api/platform/config-sets/{configSetId}/entries/{key}/reveal` | 有权限地读取敏感值 | 全局管理员或项目管理员的显式 sensitive-reveal 权限；写 `secret.revealed` 审计；不得缓存或记录值 |
| 5 | `GET/PUT /api/platform/projects/{projectId}/members` | 读取或维护项目用户/团队角色 | 仅全局管理员可管理项目范围外资源；项目管理员仅能管理所属项目成员 |
| 6 | `GET /api/platform/audit-logs` | 按对象、操作者、action、result、时间范围筛选审计 | 审计摘要严格脱敏；`SensitiveFields` 只含字段名 |
| 7 | `POST /api/platform/releases/{releaseId}/rollback` | 预检并创建新的回滚 Release | 历史版本可回滚性、目标、配置/敏感快照、镜像与锁均须校验；生产环境必须二次确认 |
| 7 | `GET /api/platform/releases/{releaseId}/rollback-diff` | 返回当前版本与目标 Release 的镜像、配置、端口和变量摘要差异 | 敏感变量只能显示名称、是否有值、hash/版本，不显示值或密文 |

### 2.5 RBAC 与 Endpoint 权限冻结

| 能力 | 全局管理员 | 项目管理员 | 开发者 | 只读观察者 |
| --- | --- | --- | --- | --- |
| 查看项目、应用、服务、发布、状态和日志 | 允许 | 所属项目且有 Endpoint 访问权时允许运行态/日志 | 所属项目且有 Endpoint 访问权时允许运行态/日志 | 所属项目且有 Endpoint 访问权时允许运行态/日志 |
| 管理环境、应用、服务与普通配置 | 允许 | 所属项目 | 仅服务部署配置且具备明确授权 | 拒绝 |
| 创建发布与历史回滚 | 允许 | 所属项目且有 Endpoint 访问权 | 所属项目、明确授权且有 Endpoint 访问权 | 拒绝 |
| 查看敏感变量元数据 | 允许 | 所属项目 | 所属项目的脱敏元数据 | 所属项目的脱敏元数据 |
| reveal/copy 或修改敏感变量 | 允许 | 所属项目且具有敏感变量管理权限 | 拒绝 | 拒绝 |
| 查看项目审计 | 允许 | 所属项目 | 拒绝 | 拒绝 |

无对象可见权的读请求返回 404；已知对象的禁止操作返回 403。所有需要目标 Docker 环境的发布、回滚、状态和日志操作还必须通过 Portainer Endpoint 权限检查。

### 2.6 阶段 2 验收与风险清单

| 验收主题 | 最小证据 |
| --- | --- |
| 三级配置与漂移 | 三级同名变量覆盖、删除/修改后的稳定 hash、当前成功 Release 的 `config-changed` 标记测试 |
| 敏感变量 | API、日志、审计和测试快照的泄漏扫描；容器收到值而 API 默认脱敏的测试 |
| 权限 | 管理员、项目管理员、开发者、观察者、无 Endpoint 权限五类矩阵测试，以及直接访问 API 的服务端拒绝测试 |
| 审计 | 配置、安全、权限、denied、回滚写入及筛选测试；审计摘要和 `SensitiveFields` 脱敏断言 |
| 人工回滚 | v1/v2 成功发布后新建回滚 Release、镜像/端口/敏感快照校验失败保持 v2 可用的测试 |
| 前端验收 | 中英文、深浅主题、长变量/镜像/审计详情、权限禁用态和确认操作的手工记录 |

主要风险是现有 Docker 发布执行器目前使用 `DesiredSpec.EnvOverrides` 构造容器环境；批次 3 与批次 4 必须将配置快照和敏感注入接入同一发布预检/执行链路，并以单元测试证明旧阶段 1 发布路径不回退。真实 Docker/Agent、浏览器主题和多用户 Endpoint 组合将在可本地验证后保留为批次 8 手工验收项。

### 2.7 批次 1 验证与结论

代码提交：`051c6429c docs: 固化阶段2实施方案`

验证命令：

```powershell
git status --short --branch
git log --oneline -n 30
rg -n -i --glob '!node_modules' --glob '!dist' "ConfigSet|ConfigEntry|SecretRefs|EffectiveConfigSnapshot|PlatformAuditLog|RollbackSourceReleaseID|RBAC" api app docs
rg -n "阶段 2 严禁|项目级 < 环境级 < 服务部署级|RollbackSourceReleaseID|SensitiveFields|Endpoint 权限|阶段 2 验收" 'docs/阶段2实施进度.md'
rg -n "[ \t]+$" 'docs/阶段2实施进度.md'
git diff --check
```

测试结果：

- 审计开始时工作区干净，最近 30 个提交没有阶段 2 实现提交；阶段 1 的完成证据完整。
- 阶段 2 关键词搜索确认核心模型和阶段 1 的最小审计已预留，ConfigSet dataservice、配置 API、密文保存、RBAC 服务、结构化审计扩展和人工回滚尚未实现。
- 阶段边界、配置合并顺序、回滚事实规则、Endpoint 权限交集、审计脱敏和最小验收项均命中冻结记录。
- 尾随空白检查无命中（`rg` 退出码 1 为无匹配），`git diff --check` 通过。

遗留风险：

- 批次 2 需要从模型预留扩展为可迁移、可导入导出的 ConfigSet dataservice，不能提前引入密文 reveal、发布注入或非管理员访问。
- 现有 Docker 执行器环境变量构造尚未消费有效配置快照；该接入留到批次 3、4，并须回归阶段 1 发布链路。

是否允许进入下一批：是。批次 1 的进度记录提交完成后，可开始批次 2 审计。

## 3. 批次 2：配置中心模型与 dataservice

提交哈希：`ffe0c2124 feat: 新增平台配置中心模型`

完成内容：

- 新增 `platform_config_sets` BoltDB dataservice，接入主 datastore、事务 datastore、测试 datastore、Export/Import 和迁移导出基准。
- 补齐 `PlatformConfigSet` 的默认构造、作用域来源归一化、作用域/键/值类型/重复键校验；配置项来源由 `project`、`environment`、`service-deployment` 作用域强制推导。
- 配置集创建默认使用 `Name=default`、`Revision=1`；只有项目、作用域、名称或 Entries 发生变化时才递增 revision，单纯逻辑归档不制造新配置版本。
- 加入敏感存储防线：在加密字段尚未实现前，敏感 `plain` 值会被校验拒绝；仅可保存无值敏感元数据或引用标识，不会把明文写入新增 bucket。
- 补充模型、CRUD/归档、事务与 Export/Import 测试，并更新迁移 JSON 基准。

验证命令：

```powershell
$env:GOCACHE = Join-Path (Get-Location) '.tmp\gocache'
E:\develop\Go\bin\go.exe test ./api/platform ./api/datastore ./api/dataservices/... -count=1
E:\develop\Go\bin\go.exe test ./api ./api/internal/testhelpers -count=1
E:\develop\Go\bin\go.exe test ./api/datastore -run 'TestMigrateData/migrate_version_24_to_latest' -count=1
rg -n -i "password|secret|token|authorization|registry@123456|minio123" api/platform_models.go api/platform_models_test.go api/dataservices/platformconfigset api/datastore/platform_datastore_test.go api/datastore/services.go
git diff --check
```

测试结果：

- `go test ./api/platform ./api/datastore ./api/dataservices/... -count=1` 通过。
- `go test ./api ./api/internal/testhelpers -count=1` 通过。
- 迁移基准用例通过；初次运行发现默认 Go cache 无权限，改用仓库内 `.tmp\gocache` 后完成编译和测试。
- 敏感词扫描只命中类型、字段名、测试元数据和既有 authorization 变量，没有真实密码、token、认证头或示例密文。
- `git diff --check` 通过。

遗留风险：

- 本批没有 ConfigSet HTTP API、三级有效配置计算、配置 hash/漂移或发布快照接入；这些必须在批次 3 完成。
- 本批故意不保存敏感 plain 值。批次 4 需要基于 Portainer 加密能力引入密文、密钥版本和脱敏响应，再将敏感值注入发布执行器。
- ConfigSet 的作用域与项目归属关联尚未由 API 层验证父对象存在性；批次 3 handler 必须补齐该校验与 `ResourceVersion` 冲突处理。

是否允许进入下一批：是。批次 3 可在不开放敏感明文和非管理员权限的前提下实现配置 CRUD、三级合并、快照与漂移。

## 4. 批次 3：配置中心 API 与配置合并

提交哈希：`2804e54ee feat: 新增平台配置中心 API`

完成内容：

- 新增管理员限定的 ConfigSet 列表、创建、详情、更新、归档 API，以及服务部署的有效配置查询 API。
- API 校验项目、环境、服务部署作用域归属；同一有效作用域内同名配置集唯一，并沿用 `ResourceVersion` 乐观锁和逻辑归档规则。
- 新增可单测的三级配置解析器：默认配置集和 `ConfigRefs` 都支持，合并顺序固定为项目级 < 环境级 < 服务部署级 `EnvOverrides`；required 引用缺失会阻止发布校验/创建。
- 发布创建和校验现在构造 `ConfigSetRevisions`、稳定排序的有效 Entries、`ConfigHash` 与完整 `EffectiveConfigSnapshot`；阶段 1 历史 Release 仍按旧路径兼容。
- 普通 plain 配置从 Release 快照注入 candidate/正式容器；敏感条目和引用条目不注入，继续等待批次 4 的加密、解密和专用注入链路。
- 修改或归档配置集后，会将存在有效快照且 hash 不一致的当前服务标记为 `config-changed`；运行资源缺失状态不会被配置漂移覆盖。

验证命令：

```powershell
$env:GOCACHE = Join-Path (Get-Location) '.tmp\gocache'
E:\develop\Go\bin\go.exe test ./api/http/handler/platform ./api/platform -count=1
E:\develop\Go\bin\go.exe test ./api ./api/datastore ./api/http/handler/platform ./api/platform -count=1
rg -n -i "password|secret|token|authorization|registry@123456|minio123" api/platform/effective_config.go api/platform/effective_config_test.go api/platform/docker_runtime_driver.go api/http/handler/platform/config_sets.go api/http/handler/platform/config_sets_test.go api/http/handler/platform/payloads.go api/http/handler/platform/releases.go
git diff --check
```

测试结果：

- 配置 API/合并测试覆盖项目、环境、服务部署三级作用域、默认配置、`ConfigRefs`、敏感条目脱敏、发布快照和配置漂移；相关 `go test` 通过。
- `go test ./api ./api/datastore ./api/http/handler/platform ./api/platform -count=1` 全部通过。
- 敏感词扫描只命中字段名、值类型和测试用引用标识，没有真实密码、token、认证头、密文或明文敏感值。
- `git diff --check` 通过。

遗留风险：

- 敏感配置当前只允许脱敏元数据或引用；没有加密值、reveal/copy、密钥版本、SecretSnapshots 或敏感发布注入，必须由批次 4 完成。
- API 仍维持管理员限定，项目角色与 Endpoint 权限交集留待批次 5；当前前端尚无配置中心页面。
- 非 default 配置集通过 `ConfigRefs` 引用时，同一层内按名称和 ID 稳定排序。后续若新增更复杂的配置编排，应显式扩展优先级协议，不能改变当前默认配置的三级顺序。

是否允许进入下一批：是。批次 4 可以在不泄漏明文的前提下接入 Portainer 加密能力、SecretSnapshots 和 Docker 敏感注入。

## 5. 批次 4：敏感变量加密、脱敏与注入

提交哈希：`ec919674d feat: 支持平台敏感变量加密注入`

完成内容：

- 复用 Portainer 已启用的加密 BoltDB 密钥，通过 AES-GCM 对敏感 plain 配置进行字段级加密；数据库未加密或密钥不可用时拒绝保存敏感值，不会降级为明文。
- `PlatformConfigEntry` 新增密文、加密版本、hash 与是否有值元数据。请求中的敏感明文只在服务端内存中完成加密，写入 dataservice 前已清空 `Value`。
- ConfigSet 响应始终移除敏感明文和密文；Release 列表/详情响应同样移除 `SecretSnapshots.CipherText`，仅在服务端执行链路保留密文。
- 新增 reveal/copy 路径，保留独立服务端管理员检查并写入 `secret.revealed` / `secret.copied` 审计；摘要只记录配置集、字段名和是否有值，`SensitiveFields` 仅含字段名。
- 发布创建/校验构造加密 `SecretSnapshots`；Docker candidate 和正式容器从当前 Release 快照解密敏感值注入环境变量，不读取当前配置，保障后续人工回滚可使用历史密文快照。
- 拒绝将含值的敏感旧 `EnvOverrides` 带入新的 Release，要求迁移到加密 ConfigSet，防止阶段 1 兼容字段继续扩散明文。

验证命令：

```powershell
$env:GOCACHE = Join-Path (Get-Location) '.tmp\gocache'
E:\develop\Go\bin\go.exe test ./api/platform ./api/http/handler/platform -count=1
E:\develop\Go\bin\go.exe test ./api ./api/datastore ./api/platform ./api/http/handler/platform -count=1
rg -n -i "password|secret|token|authorization|registry@123456|minio123" api/platform/secret_cipher.go api/platform/secret_cipher_test.go api/platform/effective_config.go api/platform/docker_runtime_driver.go api/http/handler/platform/config_secrets.go api/http/handler/platform/config_secrets_test.go api/http/handler/platform/config_sets.go api/http/handler/platform/releases.go api/platform_models.go
git diff --check
```

测试结果：

- 核心、datastore、平台 service 和 handler 回归均通过。
- 覆盖敏感值加密保存、配置/Release API 脱敏、非管理员 reveal 拒绝、reveal/copy 审计无明文、Release SecretSnapshot 密文保留，以及 Docker 解密注入。
- 明文风险扫描只命中字段名、类型、函数名和测试专用非凭据值；未发现真实密码、token、认证头或未脱敏快照。
- `git diff --check` 通过。

遗留风险：

- 当前密钥版本固定为 `boltdb-v1`；不包含外部 KMS、密钥轮换 UI 或审批流。
- `SecretRefs` 的外部引用解析尚未引入；本批支持加密 ConfigSet 值，复杂外部密钥来源不在阶段 2 范围。
- 人工历史回滚尚未实现，但 Release 已保存服务端可用的密文快照。批次 7 必须在回滚预检中验证密钥、镜像、端口和快照可用性。

是否允许进入下一批：是。批次 5 可以在保持 reveal 服务端强制校验的前提下开放项目角色与 Endpoint 权限交集。

## 6. 批次 5：基础 RBAC 与 Endpoint 权限交集

提交哈希：`d359d0084 feat: 新增平台基础 RBAC`

完成内容：

- 将 `/api/platform` 业务路由改为受限认证，并在 handler 内按实际资源强制执行项目授权；全局管理员保持全量访问。
- 启用项目用户与团队角色策略：直接用户策略优先于团队策略，多个团队按最高角色解析；支持项目管理员、开发者、只读观察者。
- 对项目、环境、应用、服务、部署、配置、制品、发布和审计查询实现对象可见性控制：无读取权限返回 404，已知对象的禁止操作返回 403。
- 发布、运行状态、日志、发布恢复和人工处理动作同时要求项目权限与 Portainer Endpoint 权限；无 Endpoint 权限不得部署、读取日志或运行态。
- 项目管理员可管理项目成员策略与配置并 reveal/copy 本项目敏感变量；开发者可登记制品、发起发布并读取日志/状态；观察者仅可查看项目资源、日志和状态。
- 项目 API 返回由服务端解析的只读权限摘要；前端据此隐藏或禁用项目创建、资源创建、制品登记、发布和人工处理入口，并补齐中英文提示。

验证命令：

```powershell
$env:GOCACHE = Join-Path (Get-Location) '.tmp\gocache'
E:\develop\Go\bin\go.exe test ./api/http/handler/platform ./api/platform -count=1
pnpm typecheck
git diff --check
```

测试结果：

- 平台 handler 与 platform service 回归通过，覆盖管理员、项目管理员、开发者、观察者、无对象读取权限、无 Endpoint 权限、制品登记、发布、日志和运行态路径。
- `pnpm typecheck` 通过；仅提示本机 Node `22.14.0` 未满足项目声明的 `^22.22.1`，TypeScript 检查正常完成。
- `git diff --check` 通过。

遗留风险：

- 当前权限摘要仅作为前端体验优化，所有最终权限判断仍在服务端；Endpoint 策略变更后以前端下一次项目查询为准刷新按钮状态。
- 缺少真实 Docker/Agent 与浏览器多用户会话的组合手工验收，已由本地 Endpoint 策略与 handler 集成测试覆盖；阶段 8 继续记录真实浏览器和主题验收。

是否允许进入下一批：是。批次 6 可在现有角色与对象边界上补齐配置、权限变更与 denied 结构化审计。
