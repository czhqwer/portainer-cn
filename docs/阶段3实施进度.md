# 阶段 3 实施进度

版本：V0.5 制品驱动部署
启动日期：2026-07-12
权威方案：[阶段 3 分批实施方案与 Goal 模式提示词](./阶段3分批实施方案与Goal模式提示词.md)

## 1. 进度总览

| 批次 | 状态 | 提交哈希 | 验证结果 | 是否允许进入下一批 |
| --- | --- | --- | --- | --- |
| 批次 1：阶段 3 基线审计与制品契约冻结 | 已完成 | `94eceff94` | 通过 | 是 |
| 批次 2：制品与存储配置模型、dataservice | 已完成 | `7b3f1a4a1` | 通过 | 是 |
| 批次 3：手动上传与安全校验 | 未开始 | - | - | 否 |
| 批次 4：MinIO / S3 Compatible 制品拉取 | 未开始 | - | - | 否 |
| 批次 5：Docker tar / OCI archive 导入 | 未开始 | - | - | 否 |
| 批次 6：Java 8 Jar 受控包装镜像 | 未开始 | - | - | 否 |
| 批次 7：前端 dist 受控包装镜像 | 未开始 | - | - | 否 |
| 批次 8：registry 推送、发布接入与原始制品清理 | 未开始 | - | - | 否 |
| 批次 9：前端整合与 V0.5 验收 | 未开始 | - | - | 否 |

## 2. 批次 1：阶段 3 基线审计与制品契约冻结

### 2.1 批次审计

目标是先冻结 V0.5 的受支持制品、数据边界、状态和验收方式，再进入模型与实现；本批不修改业务代码、不连接真实对象存储、Docker 或 registry，也不改变阶段 2 的权限、审计和回滚行为。

依赖已经满足：阶段 2 的批次 1 至批次 8 都有独立提交和本地验证证据，最终验收记录为 `a02ccf546` 与 `e6e92ee21`。审计开始时分支为 `codex/platform-phase3`，工作区无未提交改动；最近的 `88a4a287e docs: 新增阶段3实施方案` 只提供方案，不构成任一阶段 3 批次完成证据。

审计确认的可复用基础如下：

- `PlatformArtifact`、`PlatformArtifactSnapshot` 已有类型、来源、文件元数据、SHA256、存储引用、保留标记、registry、镜像 digest 和可追溯性字段；当前 handler 只允许登记 `image-reference`，不处理文件或构建状态。
- `platform_artifacts` dataservice、迁移/导出导入基线和归档语义已存在；尚无对象存储配置模型、制品任务状态、上传会话或原始制品安全清理引用查询。
- `ReleaseExecutor`、Docker 单机驱动、Release 锁、幂等、不可变 Artifact/Config/Secret 快照、失败隔离和人工回滚已经存在。阶段 3 只向它提供成功推送后的最终镜像，绝不重写该链路。
- 项目角色、Endpoint 权限交集、`PlatformAuditLog`、敏感配置加密与前端 Permissions 摘要已经存在。阶段 3 的所有新 handler 必须复用这些服务端边界；前端禁用仅是体验优化。
- 仓库具备 Portainer registry、Docker image load/build/push 的通用能力，但平台层没有 S3/MinIO adapter、archive 导入器、受控 Jar/dist 构建器或 registry 推送器。它们都属于后续批次，不能绕过下列契约直接调用。

并行改动审计：本批开始与提交前均要求工作区干净；未发现需要暂存、回滚或吸收的用户并行改动。

### 2.2 V0.5 范围与排除项

唯一支持的输入为 Java 8 Jar、前端 dist ZIP、Docker image tar、OCI image archive，以及从受管 MinIO/S3 Compatible 读取的上述文件。每个成功路径的输出都是一个镜像；平台不保存源码、不编译源码，也不把原始制品直接作为运行时输入。

本阶段明确不实现中心 Nginx、域名、证书、网关切流、多 Docker 主机、跨主机分批发布、Kubernetes、Git、源码构建、用户 Dockerfile、用户 shell、内置 registry、完整 CI/CD、审批流、漏洞扫描、Python 源码包/wheel、SSR/Node 构建或独立阿里云 OSS adapter。阿里云 OSS 只允许在其 S3 Compatible endpoint 可用时接入；不能以独立 SDK 规避本边界。

### 2.3 文件、解压与类型识别契约

所有限额在服务端强制执行，Content-Type 只用于提示，不参与放行。限额使用实际流式读取字节数，而不是客户端声明的长度。

| 输入 | 原始文件上限 | 解压后上限 | 文件数上限 | 识别与必备条件 | 拒绝 reason |
| --- | ---: | ---: | ---: | --- | --- |
| Java 8 Jar | 1 GiB | 不适用 | 不适用 | ZIP/JAR 结构、存在 `META-INF/MANIFEST.MF`，且服务类型为 `java-service` | `ARTIFACT_TYPE_INVALID`、`ARTIFACT_SIZE_EXCEEDED` |
| 前端 dist ZIP | 512 MiB | 2 GiB | 50,000 | 安全 ZIP；SPA 必须有根 `index.html`，MPA/静态站点必须有受控入口 | `DIST_ENTRY_MISSING`、`ARCHIVE_UNSAFE` |
| Docker image tar | 10 GiB | 20 GiB | 50,000 | 受限 TAR，存在 Docker `manifest.json`、config 和合法 layer | `ARCHIVE_MANIFEST_INVALID`、`ARCHIVE_UNSAFE` |
| OCI image archive | 10 GiB | 20 GiB | 50,000 | 受限 TAR，存在 `oci-layout`、`index.json`、合法 manifest/config/layer | `ARCHIVE_MANIFEST_INVALID`、`ARCHIVE_UNSAFE` |

所有 archive/ZIP 校验器必须拒绝绝对路径、`..` 路径穿越、符号链接、硬链接、设备文件、稀疏文件、重复或超限条目、异常 layer 引用和解压大小超限；不能依赖解压工具的默认行为。校验、导入或包装失败时必须删除临时文件、临时解压目录和候选镜像，不能产生可部署 Artifact。

SHA256 始终对收到或下载的原始字节流计算，保存为小写 64 位十六进制。用户提供期望 SHA256 时必须精确匹配；不匹配即拒绝持久化和后续构建。`Traceability=strong` 至少要求原始 SHA256 或最终 digest；最终可部署 Artifact 必须同时记录原始 SHA256（适用时）、最终 image、标准 tag、digest、来源和模板/导入可追溯信息。

### 2.4 存储、凭据与保留契约

对象存储仅支持全局管理员管理的 `s3-compatible` 配置：endpoint、region、bucket、path prefix、TLS 选项和加密凭据元数据。access key、secret key、签名 URL 与服务器真实路径不得写入未加密 BoltDB 字段、API 响应、错误、审计、日志、toast 或测试快照；读取配置也必须返回脱敏 DTO。

`StoragePath` 仅可保存 provider 作用域内的逻辑对象键，绝不保存本机绝对路径、临时目录或签名 URL；普通 API 默认不返回它。临时文件地址只能驻留于进程内或受控临时目录，并在任务终止时清除。

原始制品的默认值是 `Retained=true`、`Cleanable=false`。只有构建/导入/推送终态成功、没有进行中制品任务、没有历史 Release/回滚/审计追溯引用且保留策略允许时，预检才可将其标记为可清理。清理只删除原始对象或本地原始文件并把 `Retained` 置为 false；不得删除 Artifact、Release、快照、SHA256、最终 tag/digest 或审计事实。

### 2.5 受控构建、tag 与状态契约

Jar 仅使用平台维护的 Java 8 基础镜像、固定工作目录 `/opt/app`、固定文件名 `app.jar` 与固定启动形式 `java <受限 JVM 参数> -jar /opt/app/app.jar <受限应用参数>`。dist 仅使用平台维护的静态 Nginx 基础镜像和生成的容器内站点配置；SPA 仅在容器内配置 `index.html` fallback，MPA/静态站点不注入 fallback。用户永远不能提供 Dockerfile、构建脚本、命令、基础镜像、任意 Nginx 配置或 Node/SSR 参数。

制品生命周期冻结为：`uploaded`/`fetched` → `validated` → `importing` 或 `building` → `built` → `pushing` → `ready`；任一失败进入 `failed`，记录脱敏 reason，只有显式重试才可开始新的任务。归档 Artifact 不得创建新任务或发布。构建、导入、推送分别拥有 Artifact 级任务锁；与 Release 锁独立，handler 事务只写入事实和锁，不在事务内做 Docker、对象存储或 registry I/O。

标准 tag 的唯一格式为 `<registry-namespace>/<project-slug>/<service-slug>:<artifact-version>-<artifact-id>`。namespace、project/service slug 与 version 都须按受限字符集规范化，冲突不能覆盖已有非本 Artifact tag，必须返回 `REGISTRY_TAG_CONFLICT`。push 成功且取得 digest 后才可进入 `ready`，再由既有 Release 预检和 `ReleaseExecutor` 使用该 image+digest。

统一 reason 至少包括：`ARTIFACT_TYPE_INVALID`、`ARTIFACT_SIZE_EXCEEDED`、`SHA256_MISMATCH`、`ARTIFACT_PERMISSION_DENIED`、`STORAGE_ACCESS_FAILED`、`STORAGE_OBJECT_NOT_FOUND`、`STORAGE_DOWNLOAD_TIMEOUT`、`ARCHIVE_UNSAFE`、`ARCHIVE_MANIFEST_INVALID`、`ARCHIVE_ARCHITECTURE_UNSUPPORTED`、`IMAGE_IMPORT_FAILED`、`CONTROLLED_BUILD_FAILED`、`REGISTRY_AUTH_FAILED`、`REGISTRY_TLS_FAILED`、`REGISTRY_PUSH_FAILED`、`REGISTRY_DIGEST_UNAVAILABLE`、`REGISTRY_TAG_CONFLICT`、`ARTIFACT_CLEANUP_BLOCKED` 和 `ARTIFACT_CLEANUP_FAILED`。reason 只表达场景，不能包含原始制品内容、凭据、真实路径或签名 URL。

### 2.6 API、权限与审计契约

后续批次在保持既有 `/api/platform` 风格的前提下，使用以下资源边界：

| 资源 | API | 服务端授权与限制 |
| --- | --- | --- |
| 存储配置 | `GET/POST /artifact-storages`、`GET/PUT/DELETE /artifact-storages/{id}`、`POST /artifact-storages/{id}/test` | 仅全局管理员；响应脱敏、凭据加密 |
| 手动上传 | `POST /artifacts/upload` | 项目制品写权限；归属、大小、类型、hash 和临时清理强校验 |
| 对象存储拉取 | `POST /artifacts/from-storage` | 已授权项目与受限 storage/path；不接受任意外部 URL |
| 准备镜像 | `POST /artifacts/{id}/prepare-image` | 项目制品写权限；Docker 动作还须 Endpoint 权限交集 |
| registry 推送 | `POST /artifacts/{id}/push` | 项目制品写权限与目标 Endpoint 权限交集；只允许受管 registry |
| 原始制品清理 | `POST /artifacts/{id}/cleanup-original` | 项目管理权限；先做引用预检 |
| 查询 | `GET /artifacts`、`GET /artifacts/{id}` | 项目可见性；默认脱敏，按项目/应用/服务/类型/来源/状态筛选 |

每次获取、校验、导入、构建、推送、清理和发布必须写入 `PlatformAuditLog`。审计摘要仅可使用 ID、文件名、类型、大小、SHA256、来源、模板版本、镜像引用、digest、状态与 reason；敏感数据只记录字段名称。发布继续保存不可变 ArtifactSnapshot、ConfigSnapshot 与 SecretSnapshots，失败不得改变当前线上 Release。

### 2.7 批次验收矩阵与外部演练

| 批次 | 最小自动化证据 | 真实环境待演练 |
| --- | --- | --- |
| 2 | 模型、迁移、CRUD、归档、Export/Import、脱敏与引用保护测试 | 无 |
| 3 | 权限、大小、类型、SHA256、伪造路径和临时文件清理测试 | 浏览器上传与多角色会话 |
| 4 | fake S3 adapter 的路径、超时、大小、hash、脱敏和半成品清理测试 | MinIO/S3 endpoint 连通与下载 |
| 5 | TAR/OCI 安全校验、fake Docker 导入和候选镜像清理测试 | Docker load/inspect 演练 |
| 6 | Java 模板白名单、恶意参数拒绝、fake build/清理测试 | Java 8 受控镜像构建 |
| 7 | SPA/MPA/恶意 ZIP、受控 Nginx 模板与清理测试 | 静态镜像构建与容器访问 |
| 8 | tag/digest、push 失败隔离、Release 接入、清理引用预检测试 | 私有 registry push 与单机发布 |
| 9 | Go 回归、TypeScript、i18n、权限/脱敏和验收文档 | 真实 Docker、MinIO、registry、多角色、深浅色主题 |

当前工作区未发现监听 `8999` 或 `9000` 的服务，也未确认可用的 Docker、MinIO/S3 或私有 registry。因此后续先以 fake adapter 和本地测试覆盖安全边界；每一批会把相应真实演练项与结果写入本文件，不能用模拟成功代替真实环境证据。

### 2.8 本批验证与结论

验证命令：

```powershell
git status --short --branch
git log --oneline -n 30
rg -n -i --glob '!node_modules' --glob '!dist' 'PlatformArtifact|ArtifactSnapshot|StorageProvider|StoragePath|SHA256|Retained|Cleanable|RegistryID|ImageDigest|ReleaseExecutor|RBAC|PlatformAuditLog' api app docs
git diff --check
```

验证结果：基线无未提交改动；阶段 2 的八个批次与验收记录完整；搜索确认现有字段和权限/执行器边界可复用，同时确认阶段 3 的存储配置、上传、S3、archive、受控构建、推送、清理和前端闭环均未实现。本批冻结的范围、限额、模板、状态、reason、清理、审计和 API 契约可作为后续实现门禁。

遗留风险：真实 Docker、MinIO/S3、registry、浏览器主题与多用户会话尚未提供，必须按后续批次的 fake 测试和部署环境演练清单补齐。

是否允许进入下一批：是。`94eceff94 docs: 固化阶段3制品实施方案` 已提交；修正 Markdown 行尾空格后，`git diff --check`、基线审计和关键字检索均通过。后续批次必须继续遵守本契约。

## 3. 批次 2：制品与存储配置模型、dataservice

提交哈希：`7b3f1a4a1 feat: 新增平台制品存储模型`

本批先审计并确认现有 `PlatformArtifact`、`platform_artifacts` dataservice、Release 快照、Export/Import 和阶段 2 的加密/RBAC 基础可复用，但缺少制品处理状态、任务锁字段和受管对象存储配置。工作区在开始时干净，未发现用户并行改动；本批未新增上传 API、S3 网络访问、archive 导入、Docker 构建或 registry 推送。

完成内容：

- 新增受管 `s3-compatible` 的 `PlatformArtifactStorage`、`platform_artifact_storages` bucket 与 dataservice，包含 endpoint、region、bucket、逻辑 path prefix、TLS 元数据和仅供持久化的凭据密文/版本/hash 字段。
- 凭据字段不会出现在 JSON；Export 会保留存储定位配置但主动清空密文与全部凭据元数据，Import 后必须由管理员重新填写凭据，避免备份导出泄漏。
- 扩展 `PlatformArtifact` 的 `uploaded` 至 `ready`/`failed` 状态、候选镜像、最终 tag、模板、任务租约和失败 reason 元数据；对阶段 1 的 image-reference 自动归一为 `ready`，避免破坏历史发布。
- dataservice 对类型白名单、SHA256、逻辑路径、对象存储归属、状态、保留/清理关系和凭据密文元数据进行持久化前校验；没有在事务中执行任何 Docker、对象存储或 registry I/O。
- Release ArtifactSnapshot 与回滚临时 Artifact 保留存储、tag 和模板追溯字段；新增 CRUD、事务、Export/Import、迁移基线、模型校验和 mock datastore 覆盖。

验证命令：

```powershell
$env:GOCACHE = Join-Path (Get-Location) '.tmp\gocache'
E:\develop\Go\bin\go.exe test ./api/platform ./api/datastore ./api/dataservices/... -count=1
E:\develop\Go\bin\go.exe test ./api ./api/internal/testhelpers -count=1
E:\develop\Go\bin\go.exe test ./api/http/handler/platform -count=1
rg -n -i 'access.?key|secret.?key|password|token|authorization' api/platform_models.go api/dataservices/platformartifactstorage api/datastore/services.go api/http/handler/platform/releases.go api/http/handler/platform/release_rollback.go
git diff --check
```

测试结果：全部 Go 测试通过。首次回归发现 test datastore mock 缺少新接口、导出脱敏后仍保留凭据元数据，以及 Windows 下迁移快照的字段顺序/换行基线差异；均已在本批修复后重跑。敏感词扫描只命中字段名、脱敏清空逻辑和注释，没有明文凭据、token、授权头或服务器真实路径。

遗留风险：本批仅保存加密凭据元数据，尚未接入上层加密、管理员 storage API 或 MinIO/S3 连通性测试；这些严格留给批次 4。存储配置在 Export/Import 后需要重新录入凭据是有意的安全限制。

是否允许进入下一批：是。批次 3 可在本模型契约上实现项目范围的安全上传与临时文件清理，仍不得访问对象存储、导入镜像或构建镜像。
